enum ActionType {
  block,
  attack,
  dig,
  receive,
  serve,
  tip,
  dump,
  save,
  set,
  freeBallThirdTouch,
  freeBallHandling,
  other,
}

enum ActionValue {
  point,
  pp,
  plus,
  minus,
  inGame,
  mistake,
}
