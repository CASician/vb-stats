enum ActionType {
  block,
  attack,
  dig,
  receive,
  serve,
  tip,
  dump,
  save,
  set,
  freeBallHandling,
  other,
}

enum ActionValue {
  point,
  pp,
  plus,
  minus,
  inGame,
  mistake,
}
